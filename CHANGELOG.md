# 变更记录

## 0.1.0（首发候选，尚未公开发布）

首发前修正统一纳入 0.1.0，不视为版本升级。实际发布时补充日期。

- 提供多车场景接口 `ScenarioTaskAPI`，支持公开场景信息、车辆状态、单车/批量控制、局部观测、任务交互与事件查询。
- 固定依赖 `metacar==0.4.0`，直接复用控制、挡位、位姿、道路、障碍物及向量类型，不覆盖基础包或改变原有普通输入使用习惯。
- 多车静态数据使用 `MultiVehicleSceneStaticData`，避免与基础包不同定义的同名类型混淆。
- 新增协议的必填响应缺失或格式错误时报告 `ScenarioProtocolError`，不以零值或空列表伪装正常结果。
- 共享任务对象支持 `state`，用于表达已共享对象的状态；具体可见性和任务进度由场景提供。
- 提供版本兼容、模型及响应解析测试，wheel/sdist 安装验证和公开内容检查。
- 提供 API 文档、安装说明和最小停车示例；示例不包含完整解题策略。

任务和评分规则以配套正式文档为准，不作为 SDK 内部评分实现发布。
